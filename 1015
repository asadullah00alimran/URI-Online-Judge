#include<stdio.h>
#include<math.h>
int main()
{
    float x1,x2,y1,y2;
    float distance;
    float a,b,c;

    scanf("%f %f %f %f",&x1,&y1,&x2,&y2);

    a = (x2 -x1);
    b = (y2 - y1);
    c = (a * a)+(b * b);
    distance = sqrt(c);

    printf("%.4f\n",distance);


    return 0;

}
